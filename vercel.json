{
  "version": 2,
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "echo 'Build ready'",
  "outputDirectory": ".",
  "functions": {
    "api/**/*.js": {
      "runtime": "nodejs20.x",
      "maxDuration": 30
    }
  }
}
